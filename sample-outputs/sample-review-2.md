# Claude Code Review — Sample Output 2

## PR: vercel/next.js#78901 — Fix ISR revalidation edge case with headers()

**Author:** @bug-finder

---

## 🤖 Claude Code Review

### 📋 Summary
This PR fixes a bug where `revalidate` pages using `headers()` were not triggering ISR revalidation correctly, causing stale data to be served indefinitely. The root cause was identified as a missing `cache-control` header merge in the ISR response handling code. The fix is minimal (4 lines changed across 2 files) and targets the core Next.js server runtime.

### ⚠️ Identified Risks
- **No significant risks identified**: The change is surgical and well-contained. It adds a single header merge operation only when `revalidate` and `headers()` are used together.
- **Test coverage**: The added test covers the reported case but doesn't test the interaction with `cookies()` or `searchParams()` which may have similar issues.

### 💡 Improvement Suggestions
- Consider adding a regression test that also covers `cookies()` in ISR context, as the same pattern could be affected.
- Document the known limitation in the Next.js docs: ISR pages that rely on request headers should explicitly set `revalidate` in the fetch options rather than relying on the route-level config.

### ✅ Confidence: High
The fix directly addresses the reported bug with minimal surface area. The test demonstrates the issue is resolved, and the change doesn't introduce any obvious new failure modes.

---

*Review generated by claude-review CLI v1.0.0*
